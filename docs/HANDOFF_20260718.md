# ★セッション引き継ぎ 2026-07-18（次セッションは最初にここを読む）

コンテキスト逼迫のためセッションを移行する。この内容はCosense「iFont」ページにも転記する
（本セッション末に承認基盤の障害でCosense書き込みが通らなかった場合、次セッションの最初に転記すること）。

## 次セッションが最初にやること（順序つき）

1. **未コミットの変更をコミット・pushする**（下記参照。実装・検証済みで、コミットだけが
   承認基盤の一時障害で保留になった）。push後にGitHub Pagesの反映を確認する。
2. **保留中のワークフローを起動する**: `Workflow({scriptPath: "<リポジトリ>/temp/workflow_f1f2_paper31.js"})`
   - 中身: F2（干渉判定の事前登録ルール＋検出力シミュレーション）を先に実行し、その結論を受けて
     F1（セッション設計と10万円予算配分）。並行して#31（論文へ変更履歴で話者選定・いなばくん実測・
     刺激品質管理の節を追加）。
   - 成果物: docs/prereg_interference.md、docs/session_design.md、論文docx（変更履歴）。
   - エージェントはcommitしない約束。完了後に統括がレビューしてコミットする。
3. 栗原はfracパイロットを体験中（下記）。フィードバックが来たらOpusで改修する。

## 未コミットの変更（git statusで確認済み。すべて本番化Stage1=乙課題の配管）

- 変更: experiment/pilot_soa_audio.js / pilot_soa_audio.html / pilot_soa_visual2.js /
  pilot_soa_visual2.html / gas/code.gs
- 新規: experiment/prod_common.js / experiment/PRODUCTION.md
- 内容: `?prod=1`のときだけ本番モード（同意画面→課題→各試行をGASへ保存→完了コード表示）。
  `?prod`無しは従来の研究者パイロット。prod_common.js=共通配管（作業者ID `?wid` 捕捉・12桁完了コード・
  同意画面はモダリティ中立でヘッドホン注記は課題別・SUBMIT_URLが空なら送信スキップ）。
  gas/code.gs=kind:soa_trial/soa_doneの分岐追加（soa_trialsに1試行1行・soa_sessionsに完了1行。
  frac系の従来trialsと共存）。
- 検証済み: 同意表示・worker捕捉・完了コード生成・送信スキップ・完了パスのsaveDone呼び出し（聴覚・視覚とも）。
- GASのデプロイ（SUBMIT_URL貼り付け）は栗原の作業。手順は experiment/PRODUCTION.md。

## fracパイロットの現状（栗原が体験中）

- URL（qurihara.github.io/iFont/experiment/ 配下）: 聴覚1文字 audio1char.html /
  視覚1文字 visual1char.html / 聴覚2文字 audio2char.html
- **既知の未修正欠陥（体験前に栗原へ通知済み）**: 聴覚fracの打ち切りが「スロット先頭からfrac%」のままで、
  乙で確立した「実測した音響的開始への位置合わせ＋A特性gain」が未移植。きりたんのクリップは先頭に
  0〜90msの無音があるため、小さいfracはほぼ無音になる。直し方は確立済み
  （audio1char.jsのgatedBufferに audio1char_onsets.json の acoustic_onset_ms / gain を適用。
  乙の pilot_soa_audio.js v1.8/v1.9 の実装が手本）。
- audio2charはC1全提示＋C2打ち切り。C2境界の閉鎖無音は設計上内在。
- 乙で入れた改善（クリック開始・教示・出題の配り方）も未移植。記録も旧設計（サーバ未接続・ローカルDL無し）。
- 方針: フィードバック後にOpusで移植し、frac合格水準になったら乙と同じ配管（prod_common.js）で本番化。

## 直近の確定事項（このセッションで決まったこと）

- 話者=東北きりたん(id108)・B3で確定。ぱ行の字ごと処方（ぱ=C・ぴ=A・ぷ=F・ぺ=A・ぽ=I）を
  build_1char_pool.py に焼き込み済み（v3.2）。
- 2文字プール5184対もきりたん＋C2品質管理で再生成・デプロイ済み。
- 乙の再パイロット（きりたんv3.2）は分析済み: 干渉判定は S=200(83%/100%) ≧ 頭打ち(67%/75%) で良好。
  長SOAの誤りは教科書的混同のみ（破裂音の場所6・弱い開始3・鼻音2・清濁1・他2）。
  統括判断=**刺激調整は凍結し本番化へ**（これ以上はPI1人の耳への過適合）。
- 出力サンプルは実声版をGitHub Pagesで公開（experiment/ifont_sample.html）。

## モデル分担（栗原の方針・厳守）

分析・統括=Fable 5、実装=Opus 4.8。切り替えどきはこちらから明示的に伝える
（記憶ファイル model-usage-policy.md にもある）。

## 障害メモ

2026-07-18にツール承認基盤（コマンド安全判定）が断続的に停止し、Bash/MCP/Workflowが数分単位で
ブロックされることが繰り返された。対処: 少し待って再試行、または run_in_background 経由。

## 主要ファイル・URL早見

- 本番プール（単音）: experiment/audio1char_stimuli（きりたんv3.2・ぱ行処方込み）＋
  audio1char_manifest.json ＋ audio1char_onsets.json ＋ audio1char_votfix_B3_108.json
- 2文字: experiment/audio2char_stimuli（5184対）
- 正解表: experiment/answer_key_merged.json（git管理・話者ID込みハッシュで多プール同居）
- ビルダー: two_char_audio/build_1char_pool.py（VOT自動修正＋ぱ行処方入り）・
  build_2char_pool.py（C2品質管理入り）。onsets再計測: experiment/tools/build_onsets.py
- 乙課題: pilot_soa_audio.html（v3.2）/ pilot_soa_visual2.html。点検モード: ?check=1（話者切り替え付き）
- 聴き比べ資産: experiment/candidate_pools/（9,21,45,53,94,108,108e4,21c4,53c4,legacy_metan）・ptuning.html
- 出力サンプル: experiment/ifont_sample.html ＋ sample_assets/
- パイロット結果: temp/pilot_soa_audio_1784347651433.json（きりたんv3.2）・pilot_results/

## 残タスク一覧

- #29 乙再パイロット = 完了・分析済み（タスクは完了にしてよい）
- F1・F2 = ワークフロー起動待ち（temp/workflow_f1f2_paper31.js）
- #31 = 同ワークフロー内
- frac改修（onset移植）= 栗原フィードバック待ちでOpus
- frac本番化 = 合格水準後（prod_common.jsを流用）
- GASデプロイ = 栗原（PRODUCTION.md参照）
- 倫理審査書類（倫理審査/ フォルダ）との整合確認も本番前に一度
